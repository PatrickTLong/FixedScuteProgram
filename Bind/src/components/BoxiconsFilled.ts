import { createIconSet } from 'react-native-vector-icons';
import glyphMap from '../frontassets/boxicons/filled/fonts/regular/400/boxicons-filled.json';

const BoxiconsFilled = createIconSet(glyphMap, 'boxicons-filled', 'boxicons-filled.ttf');

export default BoxiconsFilled;
