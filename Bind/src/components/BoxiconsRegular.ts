import { createIconSet } from 'react-native-vector-icons';
import glyphMap from '../frontassets/boxicons/basic/fonts/regular/400/boxicons.json';

const BoxiconsRegular = createIconSet(glyphMap, 'boxicons', 'boxicons.ttf');

export default BoxiconsRegular;
