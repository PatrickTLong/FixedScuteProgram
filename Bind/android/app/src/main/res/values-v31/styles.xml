<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme for Android 12+ -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Splash screen logo on Android 12+ -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/heart_face</item>
        <item name="android:windowSplashScreenBackground">#28282B</item>
    </style>

    <!-- BootSplash theme for Android 12+ (keeps heart_face as system splash icon) -->
    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_heart</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/heart_face</item>
        <item name="android:windowSplashScreenBackground">#28282B</item>
    </style>

    <!-- Fullscreen overlay theme for BlockedActivity -->
    <style name="BlockedOverlayTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
